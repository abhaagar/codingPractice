#include<iostream>
#include<string>

int main() {
   std::string sentence;
   std::cin >> sentence;
   int start = -1;
   for(int i=0; i<string.len(); ++i) {
      if(start==-1)
         start = i;
      if(string[i]=='.')
         end = i-1;

   }
   char c;
   int start = 0, end = 6;
   while(start<end) {
      c = word[start];
      word[start] = word[end];
      word[end] = c;
      ++start;
      --end;
   }
   std::cout << word << std::endl;
   return 0;
}
